import React, { Component } from 'react'
import { Text, View } from 'react-native'

const ProgressScreen = () => {
    const s = require('../components/Styles'); 
        return (
            <View>
                <Text style={{fontSize:30}}> POSTĘPY </Text>
            </View>
        );
};

export default ProgressScreen;
