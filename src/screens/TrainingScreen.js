import React from 'react';
import { Text, View } from 'react-native';

const TrainingScreen = () => {
    const s = require('../components/Styles'); 
        return (
            <View>
                <Text style={{fontSize:30}}> TrainingScreen </Text>
            </View>
        );
};

export default TrainingScreen;